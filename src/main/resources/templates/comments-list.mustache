<div class="card">
    <div class="card-header bi bi-chat-dots"> {{#comments.size}}{{comments.size}}{{/comments.size}} Comments</div>

    <ul class="list-group-flush">
        {{#comments}}
            <li id="{{commentId}}" class="list-group-item">
                <span>
                    <span style="font-size: small">{{author}}</span>
                    <span style="font-size: xx-small">{{!createdDate}}</span>
                    <button id="btn-comment-update" class="badge bi bi-pencil-square"> 수정</button>
                    <button id="btn-comment-delete" class="badge bi bi-trash"> 삭제</button>
                </span>
                <div>{{content}}</div>
                <!--                <a type="button" data-toggle="collapse" data-target=".multi-collapse-{{!commentId}}"-->
                <!--                   class="bi bi-pencil-square"></a>-->
                <!--                <a type="button" onclick="main.commentDelete({{!postId}},{{!commentId}},{{!userId}},{{!user.userId}})"-->
                <!--                   class="bi bi-x-square"></a>-->

                <!--                <p class="collapse multi-collapse-{{!commentId}} show">{{!content}}</p>-->

                <!--                <form class="collapse multi-collapse-{{!commentId}}">-->
                <!--                    <input type="hidden" id="commentId" value="{{!commentId}}">-->
                <!--                    <input type="hidden" id="authorUserId" value="{{!userId}}">-->
                <!--                    <input type="hidden" id="sessionUserId" value="{{!#user}}{{!user.userId}}{{!/user}}">-->
                <!--                    <div class="form-group">-->
                <!--                        <textarea class="form-control" id="comment-content" rows="3">{{!content}}</textarea>-->
                <!--                    </div>-->
                <!--                    <button type="button" id="btn-comment-update" class="btn btn-outline-primary bi bi-pencil-square">-->
                <!--                        수정-->
                <!--                    </button>-->
                <!--                </form>-->
            </li>
        {{/comments}}
    </ul>
</div>